# Muse-style ad «florist · SMM Manager» 15 s — graphic layer

Every PNG is 1080×1920 with alpha; each element already sits at its place in the frame. Lay each layer over the whole frame, without moving it.
Timing and boxes: `SPEC.json`.
- 1.0 s: `plate-1`
- 3.0 s: `plate-1-done` replaces `plate-1`; `plate-2` appears
- 5.0 s: `plate-2-done` replaces `plate-2`; `plate-3` appears
- The stack stays until 9.0 s.
- `disclosure` from 10.0 s to the end.
- `title` (with the SP mark) from 12.0 s.

Stack position: x 72, y 300 / 460 / 584, width 936. If the florist's face falls inside the stack box in scenes 1–2, Mira moves it with one number (`Y0` in `build_layers.py`, SMM-Hub `design/exports/2026-09-23-muse-florist-15s-layers/`).
Texts are GA's edit from 23.09 15:45, the SMM Manager's duties only. Order: `DISP-MIRA-MUSE-FLORIST-15S-GRAPHIC-LAYER-PLATES-1`. Writer: Mira.
